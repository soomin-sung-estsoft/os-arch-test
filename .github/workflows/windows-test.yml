name: Windows Architecture Test

on:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  build-and-test:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up MSVC environment and build
        shell: cmd
        run: |
          set VSWHERE=%ProgramFiles(x86)%\Microsoft Visual Studio\Installer\vswhere.exe
          rem Fallback in case Visual Studio is installed under ProgramFiles.
          if not exist "%VSWHERE%" set VSWHERE=%ProgramFiles%\Microsoft Visual Studio\Installer\vswhere.exe
          if not exist "%VSWHERE%" (
            echo vswhere.exe not found
            exit /b 1
          )
          for /f "usebackq tokens=*" %%i in (`"%VSWHERE%" -latest -products * -property installationPath`) do set VSPATH=%%i
          if not defined VSPATH (
            echo Visual Studio installation path not found
            exit /b 1
          )
          call "%VSPATH%\VC\Auxiliary\Build\vcvars64.bat"
          cl /EHsc /W4 /O2 test.cpp

      - name: Run test
        shell: cmd
        run: test.exe
